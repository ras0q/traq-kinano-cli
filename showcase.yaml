type: 'runtime'

startup: |
  export CGO_LDFLAGS="`mecab-config --libs`"
  export CGO_CFLAGS="-I`mecab-config --inc-dir`"
  go mod download
  go build -o main

https: on

use_mariadb: true
entrypoint: './main'

http_proxy: 80

branch:
  master: null
  main: null
